print(">>> test_cnn.py ÇALIŞTI <<<")

import torch
import cv2
import numpy as np

print("Importlar OK")

from model import EyeStateFocusModel
print("Model class import edildi")

DEVICE = "cuda" if torch.cuda.is_available() else "cpu"
print("Kullanılan cihaz:", DEVICE)

NUM_CLASSES = 2

model = EyeStateFocusModel(num_classes=NUM_CLASSES).to(DEVICE)

model.load_state_dict(
    torch.load("eye_state_cnn.pth", map_location=DEVICE)
)

model.eval()
print("Model yüklendi ve eval modunda")

python test_cnn.py


